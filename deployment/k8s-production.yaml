apiVersion: apps/v1
kind: Deployment
metadata:
  name: kss-person-service
  namespace: kss-person-service
  labels:
    app: kss-person-service
    version: v1.0.0
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: kss-person-service
  template:
    metadata:
      labels:
        app: kss-person-service
        version: v1.0.0
    spec:
      imagePullSecrets:
      - name: docker-secret
      containers:
      - name: kss-person-service
        image: docker-hosted.sebaoffice.ir/kss-person-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:8000"
        - name: TZ
          value: "UTC"
        - name: Logging__LogLevel__Default
          value: "Information"
        - name: Logging__LogLevel__Microsoft.AspNetCore
          value: "Warning"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
          capabilities:
            drop:
            - ALL
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: kss-person-service-service
  namespace: kss-person-service
  labels:
    app: kss-person-service
  annotations:
    haproxy.org/cookie-persistence: "person-session"
spec:
  selector:
    app: kss-person-service
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
  sessionAffinity: None
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kss-person-service-ingress
  namespace: kss-person-service
  annotations:
    haproxy.org/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    acme.cert-manager.io/http01-edit-in-place: "true"

    kubernetes.io/ingress.class: haproxy
    haproxy.org/cookie-persistence: "person-session"
    haproxy.org/timeout-client: "10h"
    haproxy.org/timeout-server: "10h"

spec:
  ingressClassName: haproxy
  tls:
  - hosts:
    - person.api.sebaoffice.ir
    secretName: kss-person-service-tls
  rules:
  - host: person.api.sebaoffice.ir
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kss-person-service-service
            port:
              number: 80
